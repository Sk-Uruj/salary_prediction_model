# -*- coding: utf-8 -*-
"""Regression.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/18oVY3S4ZnUuVSV800TB-b1qkniwODEqA
"""

import pandas as pd
df = pd.read_csv("/content/Salary.csv")
display(df.head())

df.isnull().sum()

from sklearn.preprocessing import LabelEncoder

# Create a dummy categorical column for demonstration purposes
# For example, let's create a 'Gender' column based on an arbitrary condition
# (in a real scenario, this would be an existing categorical column in your data)
df['ExperienceLevel'] = df['YearsExperience'].apply(lambda x: 'Senior' if x >= 7 else ('Mid' if x >= 3 else 'Junior'))

display(df.head())

"""Now, let's apply `LabelEncoder` to the newly created `ExperienceLevel` column:"""

# Initialize the LabelEncoder
le = LabelEncoder()

# Apply Label Encoding to the 'ExperienceLevel' column
df['ExperienceLevel_Encoded'] = le.fit_transform(df['ExperienceLevel'])

# Display the DataFrame with the new encoded column and the mapping
display(df.head())
print(f"Mapping for 'ExperienceLevel': {list(le.classes_)} -> {list(le.transform(le.classes_))}")

# Define the independent variables (features)
X = df[['YearsExperience', 'ExperienceLevel_Encoded']]

# Define the dependent variable (target)
y = df['Salary']

print("Independent variables (X):")
display(X.head())

print("\nDependent variable (y):")
display(y.head())

print("Shape of X (Independent variables):")
print(X.shape)

print("\nShape of y (Dependent variable):")
print(y.shape)

print(f"X_train has {len(X_train)} rows and {len(X_train.columns)} columns.")
print(f"X_test has {len(X_test)} rows and {len(X_test.columns)} columns.")
print(f"y_train has {len(y_train)} rows.")
print(f"y_test has {len(y_test)} rows.")

# Import necessary libraries for each model
from sklearn.linear_model import LinearRegression
from sklearn.tree import DecisionTreeRegressor
from sklearn.ensemble import RandomForestRegressor
from sklearn.svm import SVR
from sklearn.neighbors import KNeighborsRegressor
from sklearn.metrics import mean_absolute_error, mean_squared_error, r2_score
import numpy as np

"""### 1. Linear Regression"""

# Initialize and train the Linear Regression model
linear_reg_model = LinearRegression()
linear_reg_model.fit(X_train, y_train)

# Make predictions
y_pred_lr = linear_reg_model.predict(X_test)

print("Linear Regression Model Trained and Predictions Made.")

"""### 2. Decision Tree Regressor"""

# Initialize and train the Decision Tree Regressor model
decision_tree_model = DecisionTreeRegressor(random_state=42)
decision_tree_model.fit(X_train, y_train)

# Make predictions
y_pred_dt = decision_tree_model.predict(X_test)

print("Decision Tree Regressor Model Trained and Predictions Made.")

"""### 3. Random Forest Regressor"""

# Initialize and train the Random Forest Regressor model
random_forest_model = RandomForestRegressor(random_state=42)
random_forest_model.fit(X_train, y_train)

# Make predictions
y_pred_rf = random_forest_model.predict(X_test)

print("Random Forest Regressor Model Trained and Predictions Made.")

"""### 4. Support Vector Machine (SVR)"""

# Initialize and train the Support Vector Regressor (SVR) model
# SVR can be sensitive to feature scaling; for demonstration, we'll use default here.
svm_model = SVR()
svm_model.fit(X_train, y_train)

# Make predictions
y_pred_svm = svm_model.predict(X_test)

print("Support Vector Regressor (SVR) Model Trained and Predictions Made.")

"""### 5. K-Nearest Neighbors (KNN) Regressor"""

# Initialize and train the K-Nearest Neighbors Regressor model
knn_model = KNeighborsRegressor()
knn_model.fit(X_train, y_train)

# Make predictions
y_pred_knn = knn_model.predict(X_test)

print("K-Nearest Neighbors Regressor Model Trained and Predictions Made.")

"""## Model Evaluation"""

# Function to evaluate model performance
def evaluate_model(y_true, y_pred, model_name):
    mae = mean_absolute_error(y_true, y_pred)
    mse = mean_squared_error(y_true, y_pred)
    rmse = np.sqrt(mse)
    r2 = r2_score(y_true, y_pred)

    print(f"--- {model_name} ---")
    print(f"Mean Absolute Error (MAE): {mae:.2f}")
    print(f"Mean Squared Error (MSE): {mse:.2f}")
    print(f"Root Mean Squared Error (RMSE): {rmse:.2f}")
    print(f"R-squared (R2): {r2:.2f}")
    print("\n")

    return {'Model': model_name, 'MAE': mae, 'MSE': mse, 'RMSE': rmse, 'R2': r2}

# Evaluate each model
results = []
results.append(evaluate_model(y_test, y_pred_lr, "Linear Regression"))
results.append(evaluate_model(y_test, y_pred_dt, "Decision Tree Regressor"))
results.append(evaluate_model(y_test, y_pred_rf, "Random Forest Regressor"))
results.append(evaluate_model(y_test, y_pred_svm, "Support Vector Regressor"))
results.append(evaluate_model(y_test, y_pred_knn, "K-Nearest Neighbors Regressor"))

# Display results in a DataFrame for easy comparison
results_df = pd.DataFrame(results)
display(results_df.sort_values(by='R2', ascending=False))

import matplotlib.pyplot as plt
import seaborn as sns

# Sort results for better visualization
results_df_sorted = results_df.sort_values(by='R2', ascending=False)

plt.figure(figsize=(12, 6))
sns.barplot(x='Model', y='R2', data=results_df_sorted, palette='viridis', hue='Model', legend=False)
plt.title('R-squared (R2) Score for Each Regression Model')
plt.xlabel('Model')
plt.ylabel('R-squared (R2) Score')
plt.ylim(min(0, results_df_sorted['R2'].min() - 0.1), 1.05) # Adjust y-axis for better visibility, especially with negative R2
plt.xticks(rotation=45, ha='right')
plt.grid(axis='y', linestyle='--', alpha=0.7)
plt.tight_layout()
plt.show()

"""This bar chart clearly illustrates the R-squared scores for each model, making it easy to compare their predictive power. As observed from the numerical evaluation, the Random Forest Regressor stands out with the highest R2 score, while the Support Vector Regressor (SVR) shows a negative R2, indicating poor performance without further tuning or preprocessing like feature scaling.

### Visualizing Model Predictions
"""

plt.figure(figsize=(15, 10))

models = {
    "Linear Regression": y_pred_lr,
    "Decision Tree Regressor": y_pred_dt,
    "Random Forest Regressor": y_pred_rf,
    "Support Vector Regressor": y_pred_svm,
    "K-Nearest Neighbors Regressor": y_pred_knn
}

plot_count = 1
for model_name, y_pred in models.items():
    plt.subplot(2, 3, plot_count) # Create a 2x3 grid of subplots
    sns.scatterplot(x=y_test, y=y_pred)
    plt.plot([y_test.min(), y_test.max()], [y_test.min(), y_test.max()], 'r--', lw=2) # Ideal prediction line
    plt.title(f'{model_name}\n(R2: {r2_score(y_test, y_pred):.2f})')
    plt.xlabel('Actual Salary (y_test)')
    plt.ylabel('Predicted Salary (y_pred)')
    plt.grid(True, linestyle='--', alpha=0.6)
    plot_count += 1

plt.tight_layout()
plt.show()

"""### Saving the Best Model"""

import pickle

# Identify the best model (Random Forest Regressor in this case)
best_model = random_forest_model

# Define the filename for saving the model
model_filename = 'best_model.pkl'

# Save the model to a .pkl file
with open(model_filename, 'wb') as file:
    pickle.dump(best_model, file)

print(f"Best model (Random Forest Regressor) saved to {model_filename}")